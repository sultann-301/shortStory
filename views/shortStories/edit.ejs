<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit shortStory</title>
  </head>
  <body>
    <form action="/shortStories/<%=story._id%>?_method=PATCH" method="POST">
      <div>
        <label for="author">Edit the name of the author</label>
        <input
          type="text"
          id="author"
          name="author"
          value="<%= story.author %>"
        />
      </div>
      <div>
        <label for="title">Edit the title</label>
        <input type="text" name="title" id="title" value="<%= story.title %>" />
      </div>
      <div>
        <label for="story">Edit the story</label>
        <textarea name="story" id="story" cols="30" rows="30">
<%= story.story %> </textarea
        >
      </div>
      <button>Submit</button>
    </form>
    <form action="/shortStories/<%= story._id %>?_method=DELETE" method="post">
      <button>DELETE</button>
    </form>

    <a href="/shortStories/<%=story._id %> ">back to story</a>
  </body>
</html>
